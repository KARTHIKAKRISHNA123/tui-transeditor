## Reviewer playbook
- One issue per line. Be specific: quote, problem, one-line fix.
- Distinguish ERRORS (must fix) from STYLE (optional) — label each.
- Never output a rewritten translation. That is out of scope.